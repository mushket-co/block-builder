{
  "name": "block-builder-example-nuxt3",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "dev": "nuxt dev",
    "dev:clean": "node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('.nuxt', { recursive: true, force: true })\" && nuxt prepare && nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "predev": "nuxt prepare"
  },
  "dependencies": {
    "@mushket-co/block-builder": "file:../..",
    "autobind-decorator": "^2.4.0",
    "jodit": "^4.7.4",
    "nuxt": "^3.16.0",
    "typograf": "^7.6.0",
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "sass": "^1.93.2",
    "typescript": "^5.3.2"
  }
}
